// fn get_values(json: &str, key: &str) -> Option<String> {
//     let pattern = format!("\"{}\":", key);
//     if let Some(start_index) = json.find(&pattern) {
//         let after_index = &json[start_index + pattern.len()..];
//         let after_index = after_index.trim_start();
//
//         if after_index.starts_with('"') {
//             let values
//     }
// }
//
// }